// 按需全局引入 vant组件
import Vue from 'vue'
import {
    Button,
    List,
    Cell,
    Tabbar,
    TabbarItem,
    Popup,
    Switch,
    Dialog,
    Checkbox,
    Icon,
    Toast,
} from 'vant'
Vue.use(Button)
    .use(Cell)
    .use(List)
    .use(Tabbar)
    .use(TabbarItem)
    .use(Popup)
    .use(Switch)
    .use(Dialog)
    .use(Checkbox)
    .use(Icon)
    .use(Toast)